# Core Flask
Flask>=3.0.0
Flask-SocketIO>=5.3.6
Flask-CORS>=4.0.0

# USB Communication (hidapi works with the native Windows HID driver — no Zadig/libusb needed)
hidapi>=0.14.0

# WebSocket support for Flask-SocketIO in threading mode (no eventlet/gevent needed)
# Without this, Werkzeug handles the upgrade request and returns 500.
simple-websocket>=1.0.0

# Bluetooth Communication
bleak>=0.20.0

# Data Processing
numpy>=1.26.0
pandas>=2.2.0

# Utilities
python-dotenv>=1.0.0
python-dateutil>=2.8.2

# System monitoring (optional, graceful fallback if missing)
psutil>=5.9.0

# Note: Do NOT install eventlet - it conflicts with Bluetooth asyncio
# Flask-SocketIO uses threading mode for compatibility
